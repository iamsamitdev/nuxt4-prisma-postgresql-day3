## Full Stack Nuxt 4 with Prisma and PostgreSQL - Day 1

### 📋 สารบัญ

1. [การเตรียมเครื่องมือ](#การเตรียมเครื่องมือ)
2. [มีอะไรใหม่ใน Nuxt 4](#มีอะไรใหม่ใน-nuxt-4)
3. [New project structure](#new-project-structure)
4. [การสร้างโปรเจค Nuxt 4 ด้วย Bun](#การสร้างโปรเจค-nuxt-4-ด้วย-bun)
5. [Routing in Nuxt 4](#routing-in-nuxt-4)
6. [Layouts in Nuxt 4](#layouts-in-nuxt-4)
7. [Workshop dreambuddy](#workshop-dreambuddy)

## โปรแกรม (Tool and Editor) ที่ใช้อบรม

1. **Visual Studio Code**
2. **Node.js 22.x**
3. **Bun.JS 1.3.x**
4. **PostgreSQL 17/18**
5. **Git**

> แนะนำ 
> หลักสูตรนี้ใช้ Node.JS เวอร์ชั่น 22 ขึ้นไป
> หลักสูตรนี้ใช้ Bun.JS เวอร์ชั่น 1.3 ขึ้นไป
> หลักสูตรนี้ใช้ PostgreSQL เวอร์ชั่น 17 ขึ้นไป
> แนะนำให้ติดตั้งโปรแกรมเหล่านี้ก่อนเริ่มต้นการอบรม

## การตรวจสอบความเรียบร้อยของเครื่องมือที่ติดตั้งบน Windows / Mac OS / Linux

เปิด Command Prompt บน Windows หรือ Terminal บน Mac ขึ้นมาป้อนคำสั่งดังนี้

### Visual Studio Code

```bash
code --version
```
ถ้าแสดงเวอร์ชั่นของ Visual Studio Code แสดงว่าติดตั้งเรียบร้อย

### Node.js

```bash
node -v
npm -v
npx -v
```
ถ้าแสดงเวอร์ชั่นของ Node.js แสดงว่าติดตั้งเรียบร้อย

### Bun.JS

```bash
bun -v
```
ถ้าแสดงเวอร์ชั่นของ Bun.JS แสดงว่าติดตั้งเรียบร้อย

### PostgreSQL

```bash
psql --version
```
ถ้าแสดงเวอร์ชั่นของ PostgreSQL แสดงว่าติดตั้งเรียบร้อย

### Git

```bash
git --version
```
ถ้าแสดงเวอร์ชั่นของ Git แสดงว่าติดตั้งเรียบร้อย

## 1. การติดตั้งเครื่องมือเพิ่มเติม

### การติดตั้ง Visual Studio Code
สามารถติดตั้ง Visual Studio Code ได้จาก https://code.visualstudio.com/download
- แนะนำให้ติดตั้งเวอร์ชั่นล่าสุด
- เมื่อติดตั้งเสร็จแล้วให้ตรวจสอบเวอร์ชั่นด้วยคำสั่ง `code --version`

### การติดตั้ง VSCode Extensions ของ Visual Studio Code
แนะนำให้ติดตั้ง Extensions ดังนี้

1. Vue (Official) by Vue
2. Vue VSCode Snippets by sarah.drasner
3. Prisma by Prisma
4. MySQL by Database Client
5. Catppuccin for VSCode by Catppuccin
6. Catppuccin Icons for VSCode by Catppuccin

### การติดตั้ง Node.js
สามารถติดตั้ง Node.js ได้จาก https://nodejs.org/en/download/
- แนะนำให้ติดตั้ง Node.js เวอร์ชั่น 22.x ขึ้นไป
- แนะนำให้ติดตั้งแบบ LTS (Long Term Support)
- เมื่อติดตั้งเสร็จแล้วให้ตรวจสอบเวอร์ชั่นด้วยคำสั่ง `node -v` และ `npm -v`

### การติดตั้ง Bun.JS
สามารถติดตั้ง Bun.JS ได้จาก https://bun.sh/
- แนะนำให้ติดตั้ง Bun.JS เวอร์ชั่น 1.3.x ขึ้นไป
- เมื่อติดตั้งเสร็จแล้วให้ตรวจสอบเวอร์ชั่นด้วยคำสั่ง `bun -v`

### การติดตั้ง PostgreSQL
สามารถติดตั้ง PostgreSQL ได้จาก https://www.postgresql.org/download/
- แนะนำให้ติดตั้ง PostgreSQL เวอร์ชั่น 17 ขึ้นไป
- เมื่อติดตั้งเสร็จแล้วให้ตรวจสอบเวอร์ชั่นด้วยคำสั่ง `psql --version`

### การติดตั้ง Git
สามารถติดตั้ง Git ได้จาก https://git-scm.com/downloads
- แนะนำให้ติดตั้ง Git เวอร์ชั่นล่าสุด
- เมื่อติดตั้งเสร็จแล้วให้ตรวจสอบเวอร์ชั่นด้วยคำสั่ง `git --version`

## 2. มีอะไรใหม่ใน Nuxt 4

### Nuxt 4 คืออะไร?
Nuxt 4.0 คือเวอร์ชันหลักใหม่ที่เน้นความเสถียร, มุ่งเน้นการปรับปรุงประสบการณ์ของนักพัฒนา (DX), และมีการเปลี่ยนแปลงที่สำคัญ (Breaking Changes) เพื่อปรับปรุงการพัฒนาให้ดียิ่งขึ้น

### มีอะไรใหม่บ้าง? (ภาพรวม)
- 🗂️ โครงสร้างโปรเจกต์ที่คลีนขึ้น
- 🔄 การดึงข้อมูล (Data Fetching) ที่ฉลาดขึ้น
- 🔧 ประสบการณ์ TypeScript ที่ดีขึ้น
- ⚡ CLI และการพัฒนาที่รวดเร็วยิ่งขึ้น
- 🎨 อัปเดต UI Templates เริ่มต้น

### โครงสร้างโปรเจกต์ใหม่
- โดยค่าเริ่มต้น โค้ดของแอปพลิเคชันจะอยู่ในไดเรกทอรี `app/` (เช่น `app/components`, `app/pages`)
- ช่วยแยกโค้ดแอปฯ ออกจากไฟล์อื่นๆ ใน root (เช่น `node_modules/`, `nuxt.config.ts`)
- ทำให้ File Watcher ทำงานเร็วขึ้น (โดยเฉพาะบน Windows/Linux)
- ไม่ต้องกังวล! Nuxt 4 ยังคงตรวจจับและรองรับโครงสร้างแบบเดิมของ Nuxt 3 ได้ หากคุณยังไม่พร้อมย้าย

```
my-nuxt-app/
├─ app/
│ ├─ components/
│ ├─ pages/
│ └─ app.vue
├─ public/
├─ server/
└─ nuxt.config.ts
```

### การดึงข้อมูลที่ฉลาดขึ้น
ปรับปรุง useAsyncData และ useFetch:
- แชร์ข้อมูลอัตโนมัติ: Components หลายตัวที่ใช้ key เดียวกัน จะแชร์ข้อมูลกันโดยอัตโนมัติ
- ล้างข้อมูลอัตโนมัติ: มีการ Cleanup อัตโนมัติเมื่อ Component ถูก unmount
- Reactive Keys: ใช้ key ที่เป็น reactive เพื่อสั่งให้ดึงข้อมูลใหม่ได้
- ควบคุม Cache ได้ดีขึ้น: มีตัวเลือกในการควบคุมการใช้ข้อมูลที่แคชไว้ได้มากขึ้น

### ประสบการณ์ TypeScript ที่ดีขึ้น
Nuxt 4 สร้างโปรเจกต์ TypeScript แยกกันสำหรับ Context ต่างๆ:
- โค้ดแอปฯ (App Code)
- โค้ดเซิร์ฟเวอร์ (Server Code)
- โฟลเดอร์ shared/
- โค้ด Builder
- Autocompletion ที่ดีขึ้น
- การอนุมานไทป์ (Type Inference) ที่แม่นยำขึ้น
- ลดข้อผิดพลาดไทป์ที่สับสน
- ตอนนี้คุณต้องการไฟล์ tsconfig.json เพียงไฟล์เดียวใน root ของโปรเจกต์!

### CLI และการพัฒนาที่เร็วขึ้น
- Cold Starts เร็วขึ้น: เซิร์ฟเวอร์ Development (Dev Server) เริ่มต้นได้เร็วขึ้นอย่างเห็นได้ชัด
- Socket-based Communication: CLI และ Vite dev server สื่อสารกันผ่าน Internal Sockets (แทน Network Ports) ช่วยลด Overhead (โดยเฉพาะบน Windows)
- Native File Watching: ใช้ fs.watch APIs เพื่อใช้ทรัพยากรระบบน้อยลง
- Node.js Compile Cache: นำ v8 compile cache กลับมาใช้ใหม่โดยอัตโนมัติ

### อัปเดตอื่นๆ
- UI Templates ที่อัปเดต: Templates เริ่มต้น (Starters) มีรูปลักษณ์ใหม่ ปรับปรุง Accessibility และมี Titles เริ่มต้นให้
- ลบ Nuxt 2 Compatibility: ส่วนที่รองรับ Nuxt 2 ถูกลบออกจาก @nuxt/kit (ส่วนใหญ่กระทบผู้สร้าง Module)
- Cleanup: ลบ Utilities และฟีเจอร์ที่เลิกใช้งาน (Deprecated) บางส่วนออกไป

### วิธีการอัปเกรด
- ตั้งเป้าให้การอัปเกรดราบรื่นที่สุด
- คำสั่งที่แนะนำ:
    ```bash
    npx nuxt upgrade --dedupe
    ```
- เครื่องมือช่วยย้าย (Migration Tool): มี Codemod ช่วยในการย้ายโค้ดหลายขั้นตอนโดยอัตโนมัติ
    ```bash
    npx codemod@latest nuxt/4/migration-recipe
    ```

### ก้าวต่อไป (What's next?)
- Nuxt 3: จะยังคงได้รับการอัปเดต (Bug Fixes และ Backports) จนถึงสิ้นเดือนมกราคม 2026
- Nuxt 5: วางแผนที่จะปล่อยออกมาเร็วขึ้น โดยจะมาพร้อม Nitro v3, h3 v2 และ Vite Environment API
- ฟีเจอร์ในอนาคต (สำหรับ 3.x และ 4.x):
    - SSR Streaming
    - Accessibility Module
    - Fetch Caching Strategies
    - และอื่นๆ อีกมากมาย!

## 3. New project structure

### 🏗️ New Project Structure — Nuxt 4

```
my-nuxt-app/
├─ app/
│  ├─ assets/
│  ├─ components/
│  ├─ composables/
│  ├─ layouts/
│  ├─ middleware/
│  ├─ pages/
│  ├─ plugins/
│  ├─ utils/
│  ├─ app.vue
│  ├─ app.config.ts
│  └─ error.vue
├─ content/
├─ public/
├─ shared/
├─ server/
└─ nuxt.config.ts
```

#### 🧩 1. app/
เป็น “หัวใจหลัก” ของแอปทั้งหมดใน Nuxt 4 (แทน src/ เดิม)
ทุกสิ่งที่เกี่ยวข้องกับฝั่ง Frontend (Client + SSR Rendering) จะอยู่ที่นี่

#### 📁 assets/
เก็บไฟล์ Static asset ที่ต้องผ่านการประมวลผลก่อนใช้งาน เช่น
- SCSS / SASS / CSS
- รูปภาพ SVG หรือไฟล์ที่ต้องแปลงด้วย Webpack / Vite
> 💡 ตัวอย่าง: app/assets/styles/main.scss

#### 📁 components/
เก็บ Vue components ที่ใช้ซ้ำได้ทั่วทั้งโปรเจ็กต์
Nuxt จะ auto import ให้อัตโนมัติ ไม่ต้อง import ด้วยตนเอง
> 💡 ตัวอย่าง: app/components/Navbar.vue, app/components/ProductCard.vue

#### 📁 composables/
เก็บฟังก์ชัน reusable logic ที่ใช้ร่วมกัน เช่น hooks หรือ state
Nuxt จะ auto import composables ที่ขึ้นต้นด้วย use
> 💡 ตัวอย่าง:
    - useAuth.ts → จัดการ token / session
    - useFetchProducts.ts → ดึงข้อมูลสินค้าจาก API

#### 📁 layouts/
ใช้กำหนด Layout หลักของแต่ละหน้า เช่น
- default.vue → layout หลักของเว็บ
- admin.vue → layout สำหรับหลังบ้าน
> 💡 เรียกใช้ layout ได้ใน page เช่น definePageMeta({ layout: 'admin' })

#### 📁 middleware/
เก็บฟังก์ชันตรวจสอบก่อนเข้าสู่หน้า เช่น Auth, Permission, Redirect
ทำงานทั้งฝั่ง Client และ Server
> 💡 ตัวอย่าง:
    - auth.global.ts → ตรวจสอบ token ก่อนเข้า route
    - admin.ts → redirect ถ้าไม่มีสิทธิ์

#### 📁 pages/
เก็บหน้าเว็บของแต่ละ route (ใช้ file-based routing)
Nuxt จะสร้าง route ให้อัตโนมัติตามโครงสร้างไฟล์
> 💡 ตัวอย่าง:
    - pages/index.vue → /
    - pages/products/[id].vue → /products/123

#### 📁 plugins/
ใช้สำหรับติดตั้ง library ภายนอกก่อน render เช่น
- Pinia, VueUse, Axios, Supabase Client ฯลฯ
> 💡 ตัวอย่าง:
    - app/plugins/supabase.client.ts → ติดตั้ง Supabase client
    - app/plugins/pinia.ts → ติดตั้ง Pinia store

#### 📁 utils/
เก็บฟังก์ชันช่วยเหลือทั่วไป (ไม่ต้อง auto import เสมอไป)
> 💡 ตัวอย่าง: app/utils/formatDate.ts, app/utils/calcTax.ts

#### 📄 app.vue
คือ Root Component ของระบบ ใช้เป็นจุดเริ่มต้นของแอปทั้งหมด
คล้ายกับ `App.vue` ของ Vue 3

#### 📄 app.config.ts
ใช้เก็บ configuration เฉพาะฝั่ง client เช่น theme, meta, title ฯลฯ
สามารถอ่านค่าได้ด้วย `useAppConfig()`
> 💡 ตัวอย่าง:
```ts
export default defineAppConfig({
  title: 'Nuxt 4 Workshop',
  theme: { primary: '#00DC82' }
})
```

#### 📄 error.vue
ไฟล์หน้าจอแสดงข้อผิดพลาด (Error Page)
จะแสดงเมื่อเกิด error 404 หรือ server error

#### 📘 2. content/
หากติดตั้งโมดูล @nuxt/content จะใช้เก็บไฟล์ Markdown หรือข้อมูล Static Content เช่น บทความหรือเอกสาร
> 💡 ตัวอย่าง: content/posts/hello-nuxt4.md

#### 🌍 3. public/
ไฟล์ static ที่ไม่ต้องผ่านกระบวนการ build เช่น favicon, robots.txt, รูปภาพ banner
จะถูก serve โดยตรงผ่าน / URL
> 💡 ตัวอย่าง: public/favicon.ico, public/robots.txt

#### 🧱 4. shared/
📦 จุดใหม่ของ Nuxt 4!
เป็นโฟลเดอร์ใช้ร่วมกันระหว่างฝั่ง Client (app/) และ Server (server/)
เหมาะสำหรับเก็บ TypeScript interfaces, validation schemas, constants, enums, หรือ utility ที่ใช้ทั้งสองฝั่ง
> 💡 ตัวอย่าง:
    - shared/types/product.ts → TypeScript interfaces
    - shared/validation/userSchema.ts → Zod validation schemas
    - shared/constants/index.ts → ค่าคงที่ (constants)

#### ⚙️ 5. server/
ฝั่ง Backend ของ Nuxt 4
เป็น directory ที่รวม API routes, server middleware, และ server utilities
Nuxt จะรันผ่าน Nitro Engine ทำให้สามารถ Deploy ได้ทุกที่ (Vercel, Cloudflare, Node, Bun, etc.)

> 💡 ตัวอย่าง:
```
server/
├─ api/
│  ├─ products.get.ts
│  ├─ products.post.ts
│  └─ auth/login.post.ts
├─ middleware/
│  └─ auth.ts
└─ utils/
   └─ db.ts
```

#### 📄 6. nuxt.config.ts
ไฟล์ config หลักของโปรเจ็กต์ เช่น
- กำหนด modules ที่ใช้
- ตั้งค่า build options
- กำหนด runtime config
- ตั้งค่า global CSS
- ตั้งค่า plugins
> 💡 ตัวอย่าง:
```ts
export default defineNuxtConfig({
  modules: ['@nuxtjs/tailwindcss', '@prisma/nuxt'],
  runtimeConfig: {
    public: { apiBase: '/api' }
  }
})
```
#### 🔍 สรุปแนวคิดใหม่ใน Nuxt 4
| แนวคิด             | ความหมาย                                        |
| :----------------- | :---------------------------------------------- |
| Unified Structure  | รวม frontend + backend ใน repo เดียว            |
| Shared Directory   | ใช้ไฟล์ร่วมกันระหว่าง client/server             |
| Simplified Imports | auto import ครอบคลุมทั้ง app และ composables    |
| Scalable by Design | แยกเลเยอร์ชัดเจน รองรับระบบใหญ่                 |
| Full Stack Ready   | เขียน API และ frontend ในโครงสร้างเดียวได้ทันที |

## 4. การสร้างโปรเจค Nuxt 4 ด้วย Bun

### ขั้นตอนที่ 1: สร้างโฟลเดอร์โปรเจคใหม่
เปิด Command Prompt (Windows) หรือ Terminal (Mac/Linux) ขึ้นมา แล้วรันคำสั่งดังนี้
```bash
mkdir nuxt4-prisma-postgresql
cd nuxt4-prisma-postgresql
```

### ขั้นตอนที่ 2: สร้างโปรเจค Nuxt 4 ด้วย Bun
รันคำสั่งต่อไปนี้เพื่อสร้างโปรเจค Nuxt 4 ใหม่ด้วย Bun
```bash
bun create nuxt@latest hello-nuxt

## เลือกตัวเลือกดังนี้
> Which Package Manager would you like to use? › Bun (current)
> Initialize git repository? › No
> Would you like to install any of the official modules? › No
```

### ขั้นตอนที่ 3: เปิดเข้า vsCode
```bash
code hello-nuxt -r
```

### ขั้นตอนที่ 4: รันโปรเจค Nuxt 4 ด้วย Bun
```bash
bun run dev
```

### ขั้นตอนที่ 5: เปิดเบราว์เซอร์
เปิดเบราว์เซอร์แล้วเข้าไปที่ URL: `http://localhost:3000` จะเห็นหน้าเว็บต้อนรับของ Nuxt 4 แสดงขึ้นมา

### ขั้นตอนที่ 6: ทดสอบแก้ไขหน้าเว็บ
เปิดไฟล์ `app/app.vue` แล้วแก้ไขข้อความเป็น
```vue
<script setup lang="ts">
  useHead({
    htmlAttrs: {
      lang: 'en',
    },
    title: 'My Nuxt 3 Application',
    meta: [
      { name: 'description', content: 'This is my Nuxt 3 application.' },
      { name: 'viewport', content: 'width=device-width, initial-scale=1' }
    ],
  })
</script>

<template>
  <div>
    <!-- <NuxtRouteAnnouncer />
    <NuxtWelcome /> -->
    <h1>Welcome to My Nuxt 3 Application</h1>
  </div>
</template>
```
### ขั้นตอนที่ 7: ทดสอบ Build โปรเจค
```bash
bun run build
```

### ขั้นตอนที่ 8: รันโปรเจคหลังจาก Build
```bash
bun run preview
```

### ขั้นตอนที่ 9: เปิดเบราว์เซอร์
เปิดเบราว์เซอร์แล้วเข้าไปที่ URL: `http://localhost:3000` จะเห็นหน้าเว็บต้อนรับของ Nuxt 4 แสดงขึ้นมาเช่นกัน

## 5. Routing in Nuxt 4
### File-based Routing
Nuxt 4 ใช้ระบบ file-based routing ซึ่งหมายความว่าโครงสร้างของไฟล์ในโฟลเดอร์ `app/pages/` จะกำหนดเส้นทาง (routes) ของแอปพลิเคชันโดยอัตโนมัติ

### ตัวอย่างการสร้างหน้าเว็บและเส้นทาง
#### 1. สร้างไฟล์ `app/pages/index.vue` เพื่อสร้างหน้า Home
```vue
<script setup lang="ts">
  useHead({
    title: 'Home - My Nuxt 4 Application',
    meta: [
      { name: 'description', content: 'This is the home page of our Nuxt 4 application.' }
    ],
  })
</script>

<template>
  <div>
    <h1>Welcome to the Home Page</h1>
    <p>This is the home page of our Nuxt 4 application.</p>
  </div>
</template>

<style scoped>
</style>
```

#### 2. สร้างไฟล์ `app/pages/about.vue` เพื่อสร้างหน้า About
```vue
<script setup lang="ts">
  useHead({
    title: 'About Us - My Nuxt 4 Application',
    meta: [
      { name: 'description', content: 'This is the about page of our Nuxt 4 application.' }
    ],
  })
</script>

<template>
  <div>
    <h1>About Us</h1>
    <p>This is the about page of our Nuxt 4 application.</p>
  </div>
</template>

<style scoped>
</style>
```

#### 3. แก้ไขไฟล์ `app/app.vue` เพื่อโหลด page components
```vue
<script setup lang="ts">
  useHead({
    htmlAttrs: {
      lang: 'en',
    },
    title: 'My Nuxt 3 Application',
    meta: [
      { name: 'description', content: 'This is my Nuxt 3 application.' },
      { name: 'viewport', content: 'width=device-width, initial-scale=1' }
    ],
  });
</script>

<template>
  <div>
    <ul>
      <li><NuxtLink to="/">Home</NuxtLink></li>
      <li><NuxtLink to="/about">About</NuxtLink></li>
      <li><NuxtLink to="/products/123">Product 123</NuxtLink></li>
    </ul>
    <NuxtPage />
  </div>
</template>
```

#### 4. สร้างไฟล์ `app/pages/products/[id].vue` เพื่อสร้างหน้า Product Detail ที่ใช้ Dynamic Routing
```vue
<script setup lang="ts">
    const route = useRoute();
    const productId = route.params.id;

    useHead({
        title: `Product ${productId} - My Nuxt 4 Application`,
        meta: [
        { name: 'description', content: `Details of product ${productId}.` }
        ],
    })
</script>

<template>
    <div>
        <h1>Product Detail</h1>
        <p>This is the detail page for product with ID: {{ productId }}</p>
    </div>
</template>

<style scoped>
</style>
```

### การทดสอบ Routing
1. รันโปรเจคด้วยคำสั่ง
```bash
bun run dev
```
2. เปิดเบราว์เซอร์แล้วเข้าไปที่ URL: `http://localhost:3000` จะเห็นหน้า Home
3. คลิกที่ลิงก์ "About" เพื่อไปยังหน้า About
4. คลิกที่ลิงก์ "Product 123" เพื่อไปยังหน้า Product Detail ที่แสดงรายละเอียดของสินค้าที่มี ID เป็น 123

## 6. Layouts in Nuxt 4
### การสร้าง Layouts
Layouts ใน Nuxt 4 ใช้สำหรับกำหนดโครงสร้างหลักของหน้าเว็บ เช่น header, footer, sidebar เป็นต้น

### ตัวอย่างการสร้าง Layouts
#### 1. สร้างไฟล์ `app/layouts/default.vue` เพื่อสร้าง Default Layout
```vue
<template>
  <div>
    <header>
      <h1>My Nuxt 4 Application</h1>
      <nav>
        <ul>
          <li><NuxtLink to="/">Home</NuxtLink></li>
          <li><NuxtLink to="/about">About</NuxtLink></li>
          <li><NuxtLink to="/products/123">Product 123</NuxtLink></li>
        </ul>
      </nav>
    </header>
    <main>
      <NuxtPage />
    </main>
    <footer>
      <p>&copy; 2025 My Nuxt 4 Application</p>
    </footer>
  </div>
</template>
<style scoped>
  header {
    background-color: #f8f8f8;
    padding: 1rem;
  }
  nav ul {
    list-style: none;
    display: flex;
    gap: 1rem;
  }
  footer {
    background-color: #f8f8f8;
    padding: 1rem;
    text-align: center;
  }
</style>
```

#### 2. แก้ไขไฟล์ `app/app.vue` เพื่อใช้ Default Layout
```vue
<script setup lang="ts">
  useHead({
    htmlAttrs: {
      lang: 'en',
    },
    title: 'My Nuxt 3 Application',
    meta: [
      { name: 'description', content: 'This is my Nuxt 3 application.' },
      { name: 'viewport', content: 'width=device-width, initial-scale=1' }
    ],
  })
</script>

<template>
  <NuxtLayout>
    <NuxtPage />
  </NuxtLayout>
</template>
```

#### 3. แยก Header และ Footer เป็น Components
##### สร้างไฟล์ `app/components/Header.vue`
```vue
<template>
    <header>
        <h1>My Nuxt 4 Application</h1>
        <nav>
            <ul>
                <li><NuxtLink to="/">Home</NuxtLink></li>
                <li><NuxtLink to="/about">About</NuxtLink></li>
                <li><NuxtLink to="/products/123">Product 123</NuxtLink></li>
            </ul>
        </nav>
    </header>
</template>

<style scoped>
</style>
```

##### สร้างไฟล์ `app/components/Footer.vue`
```vue
<template>
    <footer>
        <p>&copy; 2025 My Nuxt 4 Application</p>
    </footer>
</template>

<style scoped>
</style>
```

##### แก้ไขไฟล์ `app/layouts/default.vue` เพื่อใช้ Header และ Footer Components
```vue
<script setup lang="ts">
</script>

<template>
  <Header />
  <main>
    <slot />
  </main>
  <Footer />
</template>

<style scoped>
</style>
```
#### สรุปความต่างระหว่าง `<NuxtPage>` กับ `<slot>`

| ประเด็น                   | `<NuxtPage />`                  | `<slot />`                             |
| ------------------------- | ------------------------------- | -------------------------------------- |
| วัตถุประสงค์              | Render หน้าเพจปัจจุบันตาม route | รับเนื้อหา custom จากหน้าที่ใช้ layout |
| การใช้งาน                 | Layout ทั่วไป เช่น default      | Layout เฉพาะส่วน เช่น admin, auth      |
| การซ้อน layout            | ไม่เหมาะ                        | เหมาะมาก                               |
| การใช้ใน component ทั่วไป | ใช้ไม่ได้                       | ใช้ได้                                 |
| ควบคุม content จาก page   | น้อย                            | สูง                                    |

#### สถานการณ์ต่อไปนี้ที่ “ใช้ `<NuxtPage />` ไม่ได้”
##### กรณีที่ 1 — Layout ซ้อน Layout (Nested Layout)

สมมติเรามี:
```
layouts/
  admin.vue
  dashboard.vue
```
และ dashboard.vue เป็น Layout ภายใต้ admin อีกที

Layout admin.vue:
```vue
<template>
  <Sidebar />
  <slot />
</template>
```

Layout dashboard.vue:
```vue
<template>
  <DashboardHeader />
  <main>
    <slot />
  </main>
</template>
```
Page:
```vue
definePageMeta({
  layout: 'dashboard'
})
<template>
  <h1>Dashboard Home</h1>
</template>
```
กรณีนี้ ต้องใช้ slot เท่านั้น
เพราะ `<NuxtPage />` รองรับเพียง “page เดียวต่อ layout level”
แต่ layout ซ้อนหลายชั้น ต้องการ “ส่งเนื้อหาเข้า slot ของแต่ละ layout”
ถ้าใช้ `<NuxtPage />` จะทะเลาะกัน เพราะ Nuxt จะ render page ซ้ำหลายครั้ง

##### กรณีที่ 2 — Layout ที่ไม่ถูกใช้กับ Route ของ Nuxt
เช่นใช้ layout ใน component ปกติ → แบบ Custom Layout
ตัวอย่าง:
```
components/
  ModalWrapper.vue
```
แล้วเราทำ Layout แบบนี้:
```vue
<!-- components/ModalWrapper.vue -->
<template>
  <div class="modal">
    <slot />
  </div>
</template>
```
ตรงนี้ใช้ `<NuxtPage />` ไม่ได้เด็ดขาด
เพราะ `<NuxtPage />` ทำหน้าที่เฉพาะ “render page จาก router เท่านั้น”
ถ้าใช้ใน component ธรรมดา จะ error ทันที

##### กรณีที่ 3 — ใช้ Layout กับ Content ที่ไม่ใช่ Page
เช่นใน Nuxt Content:
```
content/blog/my-article.md
```
แล้วใน template ของ Markdown เราต้องการใช้ layout เฉพาะ:
```vue
<template>
  <BlogLayout>
    <ContentRenderer />
  </BlogLayout>
</template>
```
กรณีนี้ต้องใช้ `<slot />` เสมอ
เพราะเนื้อหาเป็น Markdown ไม่ใช่ Nuxt Page → `<NuxtPage />` ใช้ไม่ได้

##### กรณีที่ 4 — การสร้าง Layout แบบ Conditional Rendering
เช่นบางหน้าต้องการ full-page mode แบบพิเศษ:
```vue
<template>
  <Header v-if="!fullPage" />
  <slot />
  <Footer v-if="!fullPage" />
</template>
```
ถ้าใช้ `<NuxtPage />` ใน layout ที่ต้องซ่อนบางส่วนบาง route จะทำงานไม่ถูก

##### กรณีที่ 5 — เวลาใช้ Layout Slot อย่างเฉพาะเจาะจง (Named Slots)
เช่น layout แบบนี้:
```vue
<template>
  <Header />
  <main>
    <slot name="content" />
  </main>
  <Footer />
</template>
```
เพจแบบนี้:
```vue
<template>
  <template #content>
    <h1>Hello</h1>
  </template>
</template>
```
รูปแบบนี้ `<NuxtPage />` ทำไม่ได้ 100%

##### สรุป (แบบเข้าใจง่ายที่สุด)

| สถานการณ์                               | slot       | NuxtPage    |
| --------------------------------------- | ---------- | ----------- |
| Layout ปกติที่ใช้กับ Route              | ✔️ ใช้ได้  | ✔️ ใช้ได้   |
| Layout ซ้อนหลายชั้น                     | ✔️ ต้องใช้ | ❌ ใช้ไม่ได้ |
| Layout ที่ใช้ใน component ทั่วไป        | ✔️ ต้องใช้ | ❌ ใช้ไม่ได้ |
| Layout ของ Nuxt Content                 | ✔️ ต้องใช้ | ❌ ใช้ไม่ได้ |
| ต้องการ Named Slots                     | ✔️ ต้องใช้ | ❌ ใช้ไม่ได้ |
| ต้องการ override ส่วนของ layout บางหน้า | ✔️ เหมาะ   | ❌ ไม่เหมาะ  |

##### สรุปแบบจำง่าย
ถ้าเป็น Layout สำหรับ Page ธรรมดา → ใช้ `<NuxtPage />` หรือ `<slot />` ก็ได้
แต่ถ้าคุณต้องการความยืดหยุ่นสูง, nested layout, custom content → ต้องใช้ `<slot />` เท่านั้น


##### Best Practice
- ใช้ `<slot />` ใน layout ตลอดเวลาได้เลย และจริง ๆ แล้วนี่คือรูปแบบที่ Nuxt แนะนำอยู่แล้ว ไม่มีข้อเสียด้าน performance หรือฟีเจอร์อะไรเป็นพิเศษ
- ส่วน `<NuxtPage />` ให้ใช้ใน app.vue เป็นหลัก

## 7. Workshop dreambuddy
### ขั้นตอนที่ 1: สร้างโปรเจค Nuxt 4 ด้วย Bun
เปิด Command Prompt (Windows) หรือ Terminal (Mac/Linux) ขึ้นมา แล้วรันคำสั่งดังนี้
```bash
bun create nuxt@latest dreambuddy
## เลือกตัวเลือกดังนี้
> Which Package Manager would you like to use? › Bun (current)
> Initialize git repository? › No
> Would you like to install any of the official modules? › No
```

### ขั้นตอนที่ 2: เปิดเข้า vsCode
```bash
code dreambuddy -r
```

### ขั้นตอนที่ 3: รันโปรเจค Nuxt 4 ด้วย Bun
```bash
bun run dev
```
### ขั้นตอนที่ 4: เปิดเบราว์เซอร์
เปิดเบราว์เซอร์แล้วเข้าไปที่ URL: `http://localhost:3000` จะเห็นหน้าเว็บต้อนรับของ Nuxt 4 แสดงขึ้นมา

### ขั้นตอนที่ 5: ติดตั้ง Tailwind CSS V.4
รันคำสั่งต่อไปนี้ใน Terminal
```bash
bun add tailwindcss @tailwindcss/vite
```

Add the @tailwindcss/vite plugin to your Nuxt configuration as a Vite plugin.
เปิดไฟล์ `nuxt.config.ts` แล้วเพิ่มโค้ดดังนี้
```ts
import tailwindcss from "@tailwindcss/vite";

export default defineNuxtConfig({
  compatibilityDate: "2025-07-15",
  devtools: { enabled: true },
  vite: {
    plugins: [
      tailwindcss(),
    ],
  },
})
```

Import Tailwind CSS
สร้างไฟล์ `app/assets/css/main.css` แล้วเพิ่มโค้ดดังนี้
```css
@import "tailwindcss";
```

จากนั้นเปิดไฟล์ `nuxt.config.ts` แล้วเพิ่มโค้ดดังนี้
```ts
import tailwindcss from "@tailwindcss/vite";

export default defineNuxtConfig({
  compatibilityDate: "2025-07-15",
  devtools: { enabled: true },
  css: ['./app/assets/css/main.css'],
  vite: {
    plugins: [
      tailwindcss(),
    ],
  },
})
```
Start using Tailwind in your project
เปิดไฟล์ `app/pages/index.vue` แล้วเพิ่มโค้ดดังนี้
```vue
<template>
  <h1 class="text-3xl font-bold underline">
    Hello world!
  </h1>
</template>
```

### ขั้นตอนที่ 6: ติดตั้ง Nuxt UI
รันคำสั่งต่อไปนี้ใน Terminal
```bash
bun add @nuxt/ui
```

เปิดไฟล์ `nuxt.config.ts` แล้วเพิ่มโค้ดดังนี้
```ts
export default defineNuxtConfig({
  modules: ['@nuxt/ui']
})
```

Import Tailwind CSS and Nuxt UI in your CSS
เปิดไฟล์ `app/assets/css/main.css` แล้วเพิ่มโค้ดดังนี้
```css
@import "tailwindcss";
@import "@nuxt/ui";
```

### ขั้นตอนที่ 7: ทดสอบใช้งาน Nuxt UI
เปิดไฟล์ `app/app.vue` แล้วเพิ่มโค้ดดังนี้
```vue
<template>
  <UApp>
    <NuxtPage />
  </UApp>
</template>
```